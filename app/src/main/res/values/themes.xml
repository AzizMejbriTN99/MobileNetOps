<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.NetOps" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/navy_deep</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/navy_deep</item>
        <item name="android:navigationBarColor">@color/navy_deep</item>
    </style>

    <!-- Topbar button style - square with subtle ripple -->
    <style name="Widget.NetOps.IconButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:padding">8dp</item>
        <item name="iconTint">@color/icon_on_dark</item>
        <item name="rippleColor">@color/accent</item>
    </style>
</resources>
